# Basic Authentication - MUST BE AT THE TOP
AuthType Basic
AuthName "Studio - Restricted Access"
AuthUserFile /home/priv/.htpasswd_studio
Require valid-user

# Prevent directory listing
Options -Indexes

# Don't block uploads folder from being served
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Order allow,deny
    Allow from all
    Satisfy any
</FilesMatch>